import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.primeltd.optimusprimebusiness",
  appName: "Optimus Prime Business",
  webDir: "dist",
  bundledWebRuntime: false
};

export default config;
